/**
 * Compare two values for equality.
 *
 * @param {*} val1
 * @param {*} val2
 * @returns {boolean} Return true if values are equal, otherwise false.
 */
export const comparison = (val1, val2) => {
  return;
};

/**
 * Modify the following function body to avoid global variables.
 */
export const globals = () => {
  myObject = {
    name: 'Little Guy',
  };

  return myObject;
};

/**
 * Safely parse a string as a Base-10 integer.
 * @param {string} num A string to parse an integer value out of.
 * @returns {number} The Base-10 integer in the string.
 */
export const parseInteger = (num) => {
  return;
};

// Logical Operators

/**
 * Determine if two values are truthy.
 *
 * @param {*} a
 * @param {*} b
 * @returns {boolean} Return true if both values are truthy.
 */
export const and = (a, b) => {
  return;
};

/**
 * Determine if at least one value is truthy.
 *
 * @param {*} a
 * @param {*} b
 * @returns {boolean} Return true if either value is truthy.
 */
export const or = (a, b) => {
  return;
};

// Array Methods

/**
 * Find the index of the first occurrence of an item in an array.
 *
 * @param {number[]} arr An array of numbers.
 * @param {number} item An item to find the index of.
 * @returns {number} The index of the item or -1 if not found.
 */
export const indexOf = (arr, item) => {
  return;
};

/**
 * Add an item to the end of an array.
 *
 * @param {number[]} arr
 * @param {number} item
 * @returns {number[]} An updated array with the item added to the end.
 */
export const append = (arr, item) => {
  return;
};

/**
 * Remove an item from the end of an array.
 *
 * @param {number[]} arr
 * @returns {number[]} The updated array with the last item removed.
 */
export const truncate = (arr) => {
  return;
};

/**
 * Add an item to the beginning of an array.
 *
 * @param {number[]} arr
 * @param {number} item
 * @returns {number[]} An updated array with the item added to the beginning.
 */
export const prepend = (arr, item) => {
  return;
};

/**
 * Remove an item from the front of an array.
 *
 * @param {number[]} arr
 * @returns {number[]} The updated array with the first item removed.
 */
export const curtail = (arr) => {
  return;
};

/**
 * Merge to arrays together.
 *
 * @param {Array} arr1
 * @param {Array} arr2
 * @returns {Array} A merged array containing the contents of both arr1 and arr2.
 */
export const concat = (arr1, arr2) => {
  return;
};
