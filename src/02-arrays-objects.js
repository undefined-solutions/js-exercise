// Arrays

/**
 * Calculate the sum of all numbers in an array.
 *
 * @param {number[]} arr An array of numbers.
 * @returns {number} The sum of all numbers in the array.
 */
export const sum = (arr) => {
  return;
};

/**
 * Remove all instances of a value from an array.
 *
 * @param {number[]} arr An array of numbers.
 * @param {number} item An item to remove from the array.
 * @returns {number[]}
 */
export const remove = (arr, item) => {
  return;
};

/**
 * Insert an item into an array at the specified index.
 *
 * @param {number[]} arr An array of numbers.
 * @param {number} item A number to insert into the array.
 * @param {number} index The index where the item should be inserted.
 * @returns {number[]}
 */
export const insert = (arr, item, index) => {
  return;
};

/**
 * Find all items that are duplicated in an array and return a list of
 * duplicate items.
 *
 * @param {number[]} arr An array of numbers.
 * @returns {number[]} An array of numbers found to be duplicated.
 */
export const duplicates = (arr) => {
  return;
};

/**
 * Square each number in an array and return an array of the results.
 *
 * @param {number[]} arr An array of numbers.
 * @returns {numer[]}
 */
export const square = (arr) => {
  return;
};

/**
 * Find all occurences of an item in an array.
 *
 * @param {number[]} arr An array of numbers.
 * @param {number} target The target item to find all occurences of.
 * @returns {number[]} An array of indices where the item was found in
 *                      the array.
 */
export const findAllOccurrences = (arr, target) => {
  return;
};

// Objects

/**
 * Calls a function in the context of an object.
 *
 * @param {Function} fn
 * @param {Object} obj An object whose context the function should be
 *                      called with.
 * @returns {*} The result of calling the function in context of the object.
 */
export const alterContext = (fn, obj) => {
  return;
};

/**
 * Alter all objects of a provided type adding a "greeting" property to all instances.
 *
 * @param {Function} constructor An object constructor function.
 * @param {string} greeting A string to set as the greeting.
 */
export const alterObjects = (constructor, greeting) => {
  // alter objects here
};

/**
 * Returns an array of formatted strings for an objects own properties and their
 * value delimited by ": ".
 *
 * @example
 * // returns ['name: Optimus Prime', 'type: Autobot']
 * iterate({name: 'Optimus Prime', type: 'Autobot'});
 * @param {Object} obj
 * @returns {string[]} An array of strings for each key and it's value delimited by ": ".
 */
export const iterate = (obj) => {
  return;
};
